# ドラパルトexデッキ 開発作業ログ (DragonBomb.md)

本ファイルは、ドラパルトexデッキエージェントの開発作業ログです。

## [2026-06-23 00:00] DragonBomb をドラパルト ex 軸へ差し替え

### 1. 作業概要
- `agents_draft/DragonBomb/main.py` をドラパルト ex 軸のヒューリスティックへ全面更新。
- 基本方針は「ドラパルト ex を最優先で立てて殴る」ことにし、補助打点としてサマヨール / ヨノワールを同時に育てる評価を追加。
- サマヨール / ヨノワールの特性は、相手の残りサイド枚数が多い序盤は抑えめ、残りサイドが少ない終盤ほど優先するように変更。

### 2. 変更・追加されたファイル
| ファイル | 変更内容 |
|---------|---------|
| `agents_draft/DragonBomb/main.py` | ドラパルト ex 軸の新しい選択ロジックへ全面更新。 |
| `LOG.md` | 本変更ログを追記。 |

### 3. 検証結果
- `get_errors` で `agents_draft/DragonBomb/main.py` の構文・型エラーがないことを確認。
- 追加でドライランを実行し、対戦開始時の初期化と選択処理が落ちないかを確認予定。
